# Workspace Dependency Diagram — generated

> GENERATED by `infra/scripts/generate-dependency-diagram.mjs` from
> `docs/dependency-graph.json` — DO NOT EDIT BY HAND. Drift fails CI
> (structural-gates job). Solid = enforced edge today; dotted = `plannedEdges`
> (ADR-025 cumulative wiring: an edge moves from planned to solid in the commit
> that first consumes it). Direction: dependents point at their dependencies.
> Tooling packages (`@aca/typescript-config`, `@aca/eslint-config`) are
> dev-only and excluded by design.

```mermaid
flowchart BT
  classDef built fill:#d3f9d8,stroke:#2b8a3e,color:#000
  classDef planned fill:#fff3bf,stroke:#e8a100,color:#000,stroke-dasharray:5 5
  subgraph L0["Layer 0"]
    shared["@aca/shared<br/>packages/shared"]
  end
  subgraph L1["Layer 1"]
    config["@aca/config<br/>packages/config"]
  end
  subgraph L2["Layer 2"]
    logger["@aca/logger<br/>packages/logger"]
    database["@aca/database<br/>packages/database"]
    auth["@aca/auth<br/>packages/auth"]
    search["@aca/search<br/>packages/search"]
    email["@aca/email<br/>packages/email"]
    storage["@aca/storage<br/>packages/storage"]
    workflows["@aca/workflows<br/>packages/workflows"]
    video_engine["@aca/video-engine<br/>packages/video-engine"]
    plugin_kit["@aca/plugin-kit<br/>packages/plugin-kit"]
  end
  subgraph L3["Layer 3"]
    events["@aca/events<br/>packages/events"]
  end
  subgraph L4["Layer 4"]
    feature_flags["@aca/feature-flags<br/>packages/feature-flags"]
    billing["@aca/billing<br/>packages/billing"]
    ai["@aca/ai<br/>packages/ai"]
    ui["@aca/ui<br/>packages/ui"]
  end
  subgraph L5["Layer 5"]
    web["@aca/web<br/>packages/web"]
    api["@aca/api<br/>apps/api"]
    worker["@aca/worker<br/>packages/worker"]
    worker_plugins["@aca/worker-plugins<br/>packages/worker-plugins"]
  end
  ai --> database
  ai --> shared
  api --> config
  api --> database
  api --> events
  api --> logger
  api --> shared
  auth --> shared
  billing --> database
  billing --> events
  billing --> shared
  config --> shared
  database --> shared
  email --> shared
  events --> database
  events --> shared
  feature_flags --> database
  feature_flags --> events
  feature_flags --> shared
  logger --> config
  logger --> shared
  plugin_kit --> shared
  search --> shared
  storage --> shared
  ui --> shared
  video_engine --> shared
  web --> config
  web --> shared
  web --> ui
  worker --> ai
  worker --> auth
  worker --> billing
  worker --> config
  worker --> database
  worker --> email
  worker --> events
  worker --> feature_flags
  worker --> logger
  worker --> plugin_kit
  worker --> search
  worker --> shared
  worker --> storage
  worker --> video_engine
  worker --> workflows
  worker_plugins --> config
  worker_plugins --> logger
  worker_plugins --> plugin_kit
  worker_plugins --> shared
  workflows --> shared
  api -.planned.-> auth %% @aca/api→@aca/auth
  api -.planned.-> billing %% @aca/api→@aca/billing
  api -.planned.-> email %% @aca/api→@aca/email
  api -.planned.-> feature_flags %% @aca/api→@aca/feature-flags
  api -.planned.-> search %% @aca/api→@aca/search
  api -.planned.-> storage %% @aca/api→@aca/storage
  api -.planned.-> workflows %% @aca/api→@aca/workflows
  class shared built
  class config built
  class logger built
  class database built
  class events built
  class api built
  class auth planned
  class search planned
  class email planned
  class storage planned
  class workflows planned
  class video_engine planned
  class plugin_kit planned
  class feature_flags planned
  class billing planned
  class ai planned
  class ui planned
  class web planned
  class worker planned
  class worker_plugins planned
```

## Status at generation time

- **Built (6):** `@aca/shared`, `@aca/config`, `@aca/logger`, `@aca/database`, `@aca/events`, `@aca/api`
- **Planned (14):** `@aca/auth`, `@aca/search`, `@aca/email`, `@aca/storage`, `@aca/workflows`, `@aca/video-engine`, `@aca/plugin-kit`, `@aca/feature-flags`, `@aca/billing`, `@aca/ai`, `@aca/ui`, `@aca/web`, `@aca/worker`, `@aca/worker-plugins`

## Layer rules (recap, see docs/Dependency-Audit.md)

1. A package may only import packages listed for it in `edges`.
2. No cycles; layers strictly descending (a package depends only on lower layers).
3. Consumers import the package ROOT only; `adapters|providers|sandbox` folders
   are private to their own package (vendor-SDK zero-lock-in policy).
