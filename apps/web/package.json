{
  "name": "@aca/web",
  "version": "0.1.0",
  "private": true,
  "description": "AutoCreator AI — public web app (landing + auth + dashboard shell). Arabic-first RTL; talks to the /v1 API over Bearer JWT only (no server-side secrets; static export).",
  "scripts": {
    "build": "next build",
    "dev": "next dev -p 3100",
    "typecheck": "tsc --noEmit",
    "lint": "next lint"
  },
  "dependencies": {
    "@aca/shared": "workspace:*",
    "framer-motion": "^12.43.0",
    "lucide-react": "^1.28.0",
    "next": "15.5.22",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "catalog:"
  }
}
