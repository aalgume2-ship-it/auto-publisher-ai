import {Captions,Crop,Maximize,Scissors,ScanFace,Video} from 'lucide-react';import {PlatformShell,PageHero,ToolCard} from '../../components/creative/CreativePlatform';
const tools=[[Video,'Video Editor','Trim, sequence and prepare a real render.','/dashboard/library',true],[Captions,'Captions','Manage burned and subtitle-track captions.','/dashboard/library',true],[Crop,'Reframe','Adapt a cut for another aspect ratio.','#',false],[Scissors,'Remove Background','Isolate a subject without destructive edits.','#',false],[Maximize,'Upscale','Create a higher-resolution master.','#',false],[ScanFace,'Lip Sync','Match speech and face movement.','#',false]] as const;
export default function Edit(){return <PlatformShell><PageHero eyebrow="Creative Editing" title="Refine every output." description="A focused editing hub that exposes production tools without pretending unavailable AI operations are complete."/><div className="cp-tool-grid">{tools.map(([I,t,d,h,a])=><ToolCard key={t} icon={<I/>} title={t} description={d} href={h} available={a}/>)}</div></PlatformShell>}
